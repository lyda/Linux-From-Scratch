0.2 - March 15th, 2002

	* checklfs: When in verbose mode and detecting non-static linked program,
	  run a print_status failure as well

	* checklfs-pre5: now outputs to a file properly

	* log-install: list pruned directories, just not it's contents (so
	  /proc /root and /home for example do show up in the output)

0.1 - March 15th, 2002

	* Initial release

